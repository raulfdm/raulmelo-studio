---
type Props = {
  node: {
    children: { text: string }[];
  };
};

const [child] = Astro.props.node.children;
---

<span
  class:list={[
    `font-bold text-secondary border italic font-code whitespace-normal`,
    `border-secondary bg-[hsl(335, 78%, 90%)]`,
    `px-2 rounded-md`,
  ]}>{child.text}</span
>
